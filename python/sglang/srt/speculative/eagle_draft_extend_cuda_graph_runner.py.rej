diff a/python/sglang/srt/speculative/eagle_draft_extend_cuda_graph_runner.py b/python/sglang/srt/speculative/eagle_draft_extend_cuda_graph_runner.py	(rejected hunks)
@@ -121,7 +121,10 @@ def __init__(self, eagle_worker: EAGLEWorker):
                 )
             else:
                 self.hidden_states = torch.zeros(
-                    (self.max_num_token, self.model_runner.model_config.hidden_size),
+                    (
+                        self.max_num_token,
+                        self.model_runner.model_config.spec_hidden_size,
+                    ),
                     dtype=self.model_runner.dtype,
                 )
             self.seq_len_fill_value = (
